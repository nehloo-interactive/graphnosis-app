/privacy  /legal/privacy-policy  301
/terms    /legal/terms-of-use    301
# /download/mac and /download/win are resolved at REQUEST TIME by Cloudflare
# Pages Functions reading env vars (CURRENT_VERSION + DMG_FILENAME + WINDOWS_FILENAME)
# that release.yml's update-cloudflare job sets on every tag — that's the live
# path, so don't rely on hardcoding their version here. The static lines below
# are last-resort fallbacks; keep them roughly current. Note the published
# Windows installer is the signed MSI (build-windows uploads *.msi), not NSIS.
/download/mac  https://github.com/nehloo-interactive/graphnosis-app/releases/download/v1.13.3/Graphnosis_1.13.3_aarch64.dmg  302
/download/win  https://github.com/nehloo-interactive/graphnosis-app/releases/download/v1.13.3/Graphnosis_1.13.3_x64_en-US.msi  302
# Linux desktop builds (.AppImage + .deb) are produced by the build-linux job
# in release.yml, which copies them to these canonical names before attaching
# them to the GitHub release (so the .deb filename casing is deterministic).
/download/linux      https://github.com/nehloo-interactive/graphnosis-app/releases/download/v1.13.3/Graphnosis_1.13.3_amd64.AppImage  302
/download/linux-deb  https://github.com/nehloo-interactive/graphnosis-app/releases/download/v1.13.3/Graphnosis_1.13.3_amd64.deb        302
